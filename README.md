Shlomo Wordpress Cookbook
=======================

Replaces the official opscode wordpress cookbook, based on its version 1.2.1.

Adds the ability to use an external MySQL server, and set custom wp_config.php options.
